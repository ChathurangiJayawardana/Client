@page
@model MCMS.Pages.RoleCarryOut.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RoleCarryOuts[0].Role)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RoleCarryOuts[0].RoleGroup)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.RoleCarryOuts) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Role.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RoleGroup.Name)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
